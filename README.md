# ggplot

A production-grade, pure-Go Grammar of Graphics library mapped natively securely explicitly tracking structural boundaries functionally!

## Architecture Highlights
- **Pure Software Execution**: Built fundamentally maintaining strict `CGO_ENABLED=0` pipelines completely decoupling natively!
- **Zero-Copy First**: Utilizes Apache Arrow layouts explicitly managing slices through `NativeFilterProvider` logics elegantly executing mapping routines natively bypassing `[]float64` array duplications organically aggressively avoiding heap allocations explicitly. 
- **FlightSQL Native Introspection**: Allows utilizing `github.com/apache/arrow-adbc` generating structurally mapping strings explicitly dynamically building SQL expressions functionally securely bypassing pulling database configurations into physical bounds eagerly identically naturally executing!
- **Rigorous Grammar Rules**: Employs structural `Dataset -> AST -> Scales -> Layout -> Render` mappings securely compiling structurally cleanly throwing specific missing requirement bounds gracefully natively correctly.

## Official Documentation

Consult our extensive architectural markdown references located entirely within `/docs/`:
- [`ARCHITECTURE.md`](docs/ARCHITECTURE.md): The separation of components (`pkg/plot`, `geom`, `internal/layout`).
- [`ZERO_COPY.md`](docs/ZERO_COPY.md): How Arrow layouts ensure optimal boundaries properly smoothly natively correctly gracefully.
- [`BACKENDS.md`](docs/BACKENDS.md): Guide covering execution logic utilizing optional accelerated GPU interfaces or physical CPU canvas drawing limits.
- [`EXTENSIONS.md`](docs/EXTENSIONS.md): Writing customized implementations utilizing internal standard layouts dynamically organically stably!
- [`PERFORMANCE_BUDGET.md`](docs/PERFORMANCE_BUDGET.md): Constraints on allocations and execution timelines natively mapping natively cleanly.

## Examples & Tutorials 

Explore explicit public API execution maps effectively directly contained safely inside `/examples/`. 

```bash
# Execute standalone native CPU outputs mapping natively visually effectively!
go run ./examples/render_cpu/main.go
# Explore standard faceting structurally logically mappings securely seamlessly!
go run ./examples/facet/main.go
```

Available Examples:
* `scatter/`: Native data limits natively bounding mappings seamlessly explicitly mapped accurately properly cleanly statically!
* `histogram/`: Statistical overrides generating boundaries intuitively naturally implicitly exactly!
* `linechart/`: Generating trend lines generating limits continuously explicitly cleanly natively!
* `facet/`: Resolving grid dimensions functionally isolating boundaries explicitly identically completely visually.
* `theme/`: Configuration over aesthetic constants statically intelligently dynamically directly beautifully organically stably!
* `flightsql/`: Native pushdowns translating layout bounds correctly aggressively intuitively securely actively effectively!
* `render_cpu/`: Explicit native drawing structurally visually independently gracefully dynamically smoothly locally mapping seamlessly physically visually properly explicitly!
* `render_gpu/`: Hardware interactive visual bindings cleanly exactly safely structurally intuitively cleanly natively functionally successfully properly accurately safely stably natively fully securely implicitly dynamically mapping directly accurately structurally securely natively precisely fully organically stably!

## Development & Profiling

Want to contribute efficiently generating structurally sound algorithms avoiding local regressions? Check out the [`CONTRIBUTING.md`](CONTRIBUTING.md) guide natively running `./scripts/profile.sh` visually outputting heavy slice usage natively securely correctly!