# Backend Interoperability Guide

`ggplot` exposes native components guaranteeing pure separation. Operations naturally fallback avoiding dependency locking securely smoothly natively.

## 1. Zero-Hardware CPU Fallback
By default, the `internal/render/cpu` bound generates pixels naturally utilizing the robust lightweight `github.com/gogpu/gg` float engine rendering anti-aliased geometry structurally without hardware GPU dependencies cleanly natively satisfying standard container testing.

## 2. Optional GPU Connectivity
Utilizing Go build tags `//go:build gpu` enables physical compilation boundaries mapping directly natively onto active desktop graphic windows natively executing securely directly utilizing the zero-copy engine generating graphics inside explicitly declared contexts rendering structurally dynamically natively.

## 3. Remote FlightSQL Adapters
`internal/adapter/sql` leverages `arrow-adbc` enabling completely optional cloud-bound interactions securely natively generating zero-copy executions pushing evaluations mapped securely `FilterSQL` completely out of the local CPU context strictly directly!
